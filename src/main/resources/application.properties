spring.application.name=invoice-service

grpc.server.port=9090

# Active profile controls which TokenValidator implementation is used.
# mock-auth: MockTokenValidator (development/testing) — token format: tenantId|userId|ROLE1,ROLE2
# prod:      KeycloakTokenValidator (production)      — validates JWT against Keycloak JWKS
spring.profiles.active=mock-auth